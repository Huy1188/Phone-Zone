<!DOCTYPE html>
<html lang="en">
<head>
    <title>Quản lý Sản phẩm</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* 1. Reset & Layout */
        body { margin: 0; font-family: "Source Sans Pro", sans-serif; background-color: #f4f6f9; }
        .wrapper { display: flex; width: 100%; min-height: 100vh; }
        .main-column {
        flex: 1; /* Chiếm hết phần còn lại */
        display: flex;
        flex-direction: column; /* Xếp dọc: Header trên, Content dưới */
        overflow-x: hidden;
        }
        .content-container { flex: 1; padding: 30px; overflow-y: auto; }

        /* 2. Card Box */
        .card-box {
            background: white; padding: 20px; border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.05); border-top: 3px solid #ffc107;
        }
        h2 { text-align: center; color: #333; margin-bottom: 20px; border-bottom: 1px solid #eee; padding-bottom: 15px; }

        /* 3. THANH CÔNG CỤ (FILTER & ADD) */
        .toolbar {
            display: flex; justify-content: space-between; align-items: center;
            background: #f8f9fa; padding: 15px; border-radius: 5px; margin-bottom: 20px; border: 1px solid #e9ecef;
            flex-wrap: wrap; gap: 10px;
        }

        .filter-form { display: flex; gap: 10px; align-items: center; }
        
        select.form-select {
            padding: 8px; border: 1px solid #ced4da; border-radius: 4px; outline: none; min-width: 150px;
        }

        .btn-filter { background: #6c757d; color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; }
        .btn-filter:hover { background: #5a6268; }

        .btn-reset { color: #dc3545; text-decoration: none; font-size: 14px; margin-left: 5px; }
        .btn-reset:hover { text-decoration: underline; }

        .btn-add {
            background-color: #007bff; color: white; text-decoration: none;
            padding: 8px 15px; border-radius: 4px; font-weight: bold; display: flex; align-items: center; gap: 5px;
        }
        .btn-add:hover { background-color: #0056b3; }

        /* 4. Table */
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #dee2e6; text-align: left; padding: 12px; vertical-align: middle; }
        th { background-color: #f8f9fa; color: #333; }
        .img-preview { width: 50px; height: 50px; object-fit: cover; border-radius: 4px; border: 1px solid #ddd; }
        
        /* 5. Pagination (Phân trang) */
        .pagination { display: flex; justify-content: center; margin-top: 20px; gap: 5px; }
        .page-link {
            padding: 8px 12px; border: 1px solid #dee2e6; color: #007bff; text-decoration: none; border-radius: 4px; background: white;
        }
        .page-link:hover { background: #e9ecef; }
        .page-link.active { background: #007bff; color: white; border-color: #007bff; }
        .page-link.disabled { color: #6c757d; pointer-events: none; background: #fff; border-color: #dee2e6; }

        /* Actions */
        .btn-action { padding: 5px 10px; border-radius: 4px; text-decoration: none; font-size: 12px; color: white; font-weight: bold; margin-right: 3px;}
        .btn-edit { background-color: #ffc107; color: #333; }
        .btn-delete { background-color: #dc3545; }

    </style>
</head>
<body>

    <div class="wrapper">
        <%- include('./admin/sidebar.ejs') %>
      <div class="main-column">
        <%- include('./admin/header.ejs') %>
        <div class="content-container">
            <div class="card-box">
                <h2><i class="fas fa-box-open" style="color: #ffc107; margin-right: 10px;"></i> Quản lý Sản phẩm</h2>
                
                <div class="toolbar">
                    <form action="/system/product-manage" method="GET" class="filter-form">
                        
                        <select name="category_id" class="form-select">
                            <option value="">-- Tất cả danh mục --</option>
                            <% if(categories) { %>
                                <% categories.forEach(function(cat) { %>
                                    <option value="<%= cat.category_id %>" <%= currentFilter.category_id == cat.category_id ? 'selected' : '' %>>
                                        <%= cat.name %>
                                    </option>
                                <% }); %>
                            <% } %>
                        </select>

                        <select name="brand_id" class="form-select">
                            <option value="">-- Tất cả thương hiệu --</option>
                            <% if(brands) { %>
                                <% brands.forEach(function(brand) { %>
                                    <option value="<%= brand.brand_id %>" <%= currentFilter.brand_id == brand.brand_id ? 'selected' : '' %>>
                                        <%= brand.name %>
                                    </option>
                                <% }); %>
                            <% } %>
                        </select>

                        <button type="submit" class="btn-filter"><i class="fas fa-filter"></i> Lọc</button>
                        <a href="/system/product-manage" class="btn-reset">Xóa lọc</a>
                    </form>

                    <a href="/create-product" class="btn-add">
                        <i class="fas fa-plus"></i> Thêm sản phẩm
                    </a>
                </div>

                <table>
                    <thead>
                        <tr>
                            <th style="width: 50px;">ID</th>
                            <th>Tên sản phẩm</th>
                            <th style="width: 70px;">Ảnh</th>
                            <th>Giá (VNĐ)</th>
                            <th>Danh mục</th>
                            <th>Thương hiệu</th>
                            <th style="width: 130px; text-align: center;">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if(products && products.length > 0) { %> 
                            <% products.forEach(function(item) { %>
                            <tr>
                                <td><%= item.product_id %></td>
                                <td style="font-weight: bold;"><%= item.name %></td>
                                <td>
                                    <% if (item.image) { %>
                                        <img src="<%= item.image %>" class="img-preview" alt="Img">
                                    <% } else { %> - <% } %>
                                </td>
                                <td style="color: #28a745; font-weight: bold;">
                                    <%= new Intl.NumberFormat('vi-VN').format(item.min_price) %>
                                </td>
                                <td style="color: #007bff;"><%= item.category ? item.category.name : '-' %></td>
                                <td style="color: #6610f2;"><%= item.brand ? item.brand.name : '-' %></td>
                                <td style="text-align: center;">
                                    <a href="/edit-product?id=<%= item.product_id %>" class="btn-action btn-edit"><i class="fas fa-edit"></i></a>
                                    <a href="/delete-product?id=<%= item.product_id %>" class="btn-action btn-delete" onclick="return confirm('Xóa sản phẩm này?')"><i class="fas fa-trash"></i></a>
                                </td>
                            </tr>
                            <% }); %> 
                        <% } else { %>
                            <tr><td colspan="7" style="text-align: center; padding: 20px; color: #777;">Không tìm thấy sản phẩm nào!</td></tr>
                        <% } %>
                    </tbody>
                </table>

                <% if(totalPages > 1) { %>
                <div class="pagination">
                    <% if(currentPage > 1) { %>
                        <a href="/system/product-manage?page=<%= currentPage - 1 %>&category_id=<%= currentFilter.category_id %>&brand_id=<%= currentFilter.brand_id %>" class="page-link">« Trước</a>
                    <% } else { %>
                        <span class="page-link disabled">« Trước</span>
                    <% } %>

                    <% for(let i = 1; i <= totalPages; i++) { %>
                        <a href="/system/product-manage?page=<%= i %>&category_id=<%= currentFilter.category_id %>&brand_id=<%= currentFilter.brand_id %>" 
                           class="page-link <%= i == currentPage ? 'active' : '' %>">
                            <%= i %>
                        </a>
                    <% } %>

                    <% if(currentPage < totalPages) { %>
                        <a href="/system/product-manage?page=<%= parseInt(currentPage) + 1 %>&category_id=<%= currentFilter.category_id %>&brand_id=<%= currentFilter.brand_id %>" class="page-link">Sau »</a>
                    <% } else { %>
                        <span class="page-link disabled">Sau »</span>
                    <% } %>
                </div>
                <% } %>

            </div>
        </div>
      </div>
    </div>

</body>
</html>