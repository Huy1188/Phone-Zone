<!DOCTYPE html>
<html lang="en">
<head>
    <title>Chi tiết đơn hàng #<%= order.order_id %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* 1. Reset & Layout */
        body { margin: 0; font-family: "Source Sans Pro", sans-serif; background-color: #f4f6f9; }
        .wrapper { display: flex; width: 100%; min-height: 100vh; }
        .main-column {
        flex: 1; /* Chiếm hết phần còn lại */
        display: flex;
        flex-direction: column; /* Xếp dọc: Header trên, Content dưới */
        overflow-x: hidden;
        }
        .content-container { flex: 1; padding: 30px; overflow-y: auto; }

        /* 2. Card Box */
        .card-box {
            background: white;
            padding: 30px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
            border-top: 3px solid #17a2b8; /* Màu xanh đặc trưng trang chi tiết */
        }

        /* 3. Header */
        .page-header {
            display: flex; justify-content: space-between; align-items: center;
            border-bottom: 1px solid #eee; padding-bottom: 20px; margin-bottom: 20px;
        }
        h2 { margin: 0; color: #333; font-size: 24px; }
        
        /* 4. Grid thông tin (Chia 2 cột) */
        .info-grid { display: flex; gap: 20px; margin-bottom: 30px; }
        .info-col { flex: 1; background: #f8f9fa; padding: 20px; border-radius: 5px; border: 1px solid #e9ecef; }
        
        .info-title { 
            font-weight: bold; color: #555; margin-bottom: 15px; 
            border-bottom: 1px solid #ddd; padding-bottom: 8px; display: block; text-transform: uppercase; font-size: 13px;
        }
        .info-row { margin-bottom: 10px; font-size: 15px; display: flex; }
        .info-row b { min-width: 120px; color: #444; }

        /* 5. Bảng sản phẩm */
        table { width: 100%; border-collapse: collapse; margin-bottom: 30px; }
        th, td { border: 1px solid #dee2e6; padding: 12px; text-align: left; }
        th { background-color: #343a40; color: white; }
        tr:nth-child(even) { background-color: #f2f2f2; }
        
        /* 6. Thanh tác vụ phía dưới */
        .action-bar {
            background: #e9ecef; padding: 20px; border-radius: 5px;
            display: flex; justify-content: space-between; align-items: center;
        }

        .form-update { display: flex; align-items: center; gap: 10px; }
        select { padding: 8px; border-radius: 4px; border: 1px solid #ccc; height: 38px; min-width: 150px;}
        
        /* Buttons */
        .btn-update { 
            background: #28a745; color: white; border: none; padding: 0 20px; 
            border-radius: 4px; cursor: pointer; font-weight: bold; height: 38px; 
            display: flex; align-items: center; gap: 5px;
        }
        .btn-update:hover { background: #218838; }

        .btn-print { 
            background: #6f42c1; color: white; text-decoration: none; padding: 10px 20px; 
            border-radius: 4px; font-weight: bold; display: inline-flex; align-items: center; gap: 8px; 
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .btn-print:hover { background: #5a32a3; transform: translateY(-1px); }

        .back-link { text-decoration: none; color: #6c757d; font-weight: bold; display: inline-flex; align-items: center; gap: 5px; }
        .back-link:hover { color: #333; }

    </style>
</head>
<body>

    <div class="wrapper">
        <%- include('./admin/sidebar.ejs') %>
        <div class="main-column">
            <%- include('./admin/header.ejs') %>
            <div class="content-container">
            <div class="card-box">
                
                <div class="page-header">
                    <h2><i class="fas fa-file-invoice"></i> Chi tiết đơn hàng #<%= order.order_id %></h2>
                    <a href="/system/print-invoice/<%= order.order_id %>" target="_blank" class="btn-print">
                        <i class="fas fa-print"></i> Xuất Hóa Đơn
                    </a>
                </div>

                <div class="info-grid">
                    <div class="info-col">
                        <span class="info-title"><i class="fas fa-info-circle"></i> Thông tin chung</span>
                        <div class="info-row"><b>Ngày đặt:</b> <%= new Date(order.createdAt).toLocaleString('vi-VN') %></div>
                        <div class="info-row">
                            <b>Trạng thái:</b> 
                            <% if(order.status == 'pending') { %> <span style="color:#ffc107; font-weight:bold">Chờ xử lý</span> <% } %>
                            <% if(order.status == 'shipping') { %> <span style="color:#17a2b8; font-weight:bold">Đang giao</span> <% } %>
                            <% if(order.status == 'done') { %> <span style="color:#28a745; font-weight:bold">Hoàn thành</span> <% } %>
                            <% if(order.status == 'cancel') { %> <span style="color:#dc3545; font-weight:bold">Đã hủy</span> <% } %>
                        </div>
                        <div class="info-row">
                            <b>Tổng tiền:</b> 
                            <span style="color:#dc3545; font-weight:bold; font-size: 18px;">
                                <%= new Intl.NumberFormat('vi-VN').format(order.total_money) %> đ
                            </span>
                        </div>
                    </div>

                    <div class="info-col">
                        <span class="info-title"><i class="fas fa-map-marker-alt"></i> Giao hàng tới</span>
                        <div class="info-row"><b>Ghi chú/Tên:</b> <%= order.note %></div>
                        <div class="info-row"><b>Địa chỉ:</b> <%= order.shipping_address %></div>
                    </div>
                </div>

                <h3 style="color: #333; margin-top: 0;"><i class="fas fa-box-open"></i> Danh sách sản phẩm</h3>
                <table>
                    <thead>
                        <tr>
                            <th>Tên sản phẩm</th>
                            <th>Đơn giá</th>
                            <th>Số lượng</th>
                            <th>Thành tiền</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% orderDetails.forEach(function(item) { %>
                        <tr>
                            <td><%= item.product_name %></td>
                            <td><%= new Intl.NumberFormat('vi-VN').format(item.price) %> đ</td>
                            <td>x <%= item.quantity %></td>
                            <td style="font-weight: bold;"><%= new Intl.NumberFormat('vi-VN').format(item.price * item.quantity) %> đ</td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>

                <div class="action-bar">
                    <a href="/system/order-manage" class="back-link">
                        <i class="fas fa-arrow-left"></i> Quay lại danh sách
                    </a>

                    <form action="/system/order-update-status" method="POST" class="form-update">
                        <input type="hidden" name="order_id" value="<%= order.order_id %>">
                        
                        <label style="margin: 0; font-weight: bold; color: #555;">Cập nhật trạng thái:</label>
                        <select name="status">
                            <option value="pending" <%= order.status == 'pending' ? 'selected' : '' %>>Chờ xử lý</option>
                            <option value="shipping" <%= order.status == 'shipping' ? 'selected' : '' %>>Đang giao hàng</option>
                            <option value="done" <%= order.status == 'done' ? 'selected' : '' %>>Đã hoàn thành</option>
                            <option value="cancel" <%= order.status == 'cancel' ? 'selected' : '' %>>Đã hủy</option>
                        </select>
                        
                        <button type="submit" class="btn-update">
                            <i class="fas fa-sync-alt"></i> Cập nhật
                        </button>
                    </form>
                </div>

            </div>
            </div>
        </div>
    </div>

</body>
</html>